<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.anipick.backend.admin.mapper.AdminMapper">
    <select id="existAdminAccountUsername" resultType="boolean">
        SELECT EXISTS (SELECT 1
                FROM Adminaccount
                WHERE username = #{username}
        )
    </select>

    <select id="findByUsername" resultType="com.anipick.backend.admin.domain.Admin">
        SELECT ac.admin_id AS adminId,
               ac.username AS username,
               ac.password AS password,
               ac.is_active AS isActive,
               ac.created_at AS createdAt,
               ac.last_login_at AS lastLoginAt
        FROM Adminaccount ac
        WHERE username = #{username}
    </select>
</mapper>